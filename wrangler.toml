# Cloudflare Worker для заявок в Telegram
# Деплой: npx wrangler deploy
# Секреты: npx wrangler secret put BOT_TOKEN
#          npx wrangler secret put CHAT_ID

name = "kb-lider-leads"
main = "telegram-worker.js"
compatibility_date = "2024-01-01"

# Secrets задаются отдельно (не в репозитории):
# wrangler secret put BOT_TOKEN
# wrangler secret put CHAT_ID
