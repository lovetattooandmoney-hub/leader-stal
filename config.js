/**
 * Публичный URL HTTP-функции в VK Cloud (см. vk-cloud/README.md).
 * Заявки приходят на почту через SMTP внутри облака — не SMS и не сообщения ВК.
 * Оставьте пустую строку — формы шлют на send.php (если на хостинге есть PHP).
 */
window.FORM_SUBMIT_URL = '';
